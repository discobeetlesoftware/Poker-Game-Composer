() => {
    var titleString = '{{element.card_count}} {% if element.is_face_up %}UP{% else %}DOWN{% endif %}';
    var element = cardPile({{element.is_face_up}}, {{element.card_count}}, 'I', titleString);
    return element.pos(lastElementMaxX, 0, LEFT, TOP, elementContainer);
},
