%label{ :for => "#{key}_value", :class => 'title' } Discard
%select{ :name => 'section[][elements][][value]', :id => "#{key}_value" }
  - (1..10).each do |i|
    %option{ :selected => element.value.to_i == i }= i
card;
%label{ :for => "#{key}_option_thenDraw" } then draw
%input{ :type => 'checkbox', :name => "section[][elements][][thenDraw]", :id => "#{key}_option_thenDraw", :selected => element.options['thenDraw'] }
