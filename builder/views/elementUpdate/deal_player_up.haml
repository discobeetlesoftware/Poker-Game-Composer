%label{ :for => "#{key}_value", :class => 'title' } Up cards
%select{ :name => 'section[][elements][][value]', :id => "#{key}_value" }
  - (1..10).each do |i|
    %option{ :selected => element.card_count == i }= i
